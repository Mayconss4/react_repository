import styled from "styled-components"

export const Section = styled.body`
    display: flex; 
    background-repeat: no-repeat;  
    width: 100%;
    picture{
        img{
            width: 100%
        }
    }
`